import ServiceSelectionPage from "./ServiceSelectionPage";

/**
 * MenuPage Component
 *
 * Now serves as the top-level service selection page.
 * Users choose between food and salon services before browsing vendors.
 */
export default function MenuPage() {
  return <ServiceSelectionPage />;
}
